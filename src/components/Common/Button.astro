---
interface Props {
	text: string;
	variant?: "light" | "dark";
	href?: string;
}

const { text, variant = "light", href } = Astro.props;
---

<div
	class="px-6 py-2 rounded-full bg-white text-black font-bold hover:bg-gray-100 transition"
>
	<p>{text}</p>
</div>
